import 'package:flutter/material.dart';

/// Paleta de cores oficial do GymTap, conforme identidade visual definida
/// no Arquivo Mestre do projeto.
class AppCores {
  AppCores._();

  static const fundo = Color(0xFF121212);
  static const cards = Color(0xFF1E1E1E);
  static const bordas = Color(0xFF2A2A2A);
  static const textoPrincipal = Color(0xFFFFFFFF);
  static const textoSecundario = Color(0xFFB3B3B3);
  static const destaque = Color(0xFF3541B0);
}

/// Textos e constantes gerais reutilizados pelo app.
class AppTextos {
  AppTextos._();

  static const nomeApp = 'GymTap';
}
